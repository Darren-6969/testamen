{
  "name": "memodise",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "api": "npm --prefix api run dev",
    "backend_admin": "npm --prefix backend_admin run dev",
    "backend_customer": "npm --prefix backend_customer run dev",
    "landing_page": "npm --prefix landing_page run dev",
    "build:backend_admin": "npm --prefix backend_admin run build",
    "build:backend_customer": "npm --prefix backend_customer run build",
    "build:landing_page": "npm --prefix landing_page run build"
  }
}
